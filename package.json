{
  "name": "study-studio-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun --filter @study-studio/web dev",
    "dev:gateway": "bun --filter @study-studio/gateway dev",
    "dev:desktop": "bun --filter @study-studio/desktop dev",
    "build": "bun --filter \"*\" build",
    "build:sidecar": "bun run --filter @study-studio/gateway sidecar:build",
    "typecheck": "tsc -b --force",
    "test": "bun test",
    "release:sync": "bun scripts/release.ts sync",
    "release:notes": "bun scripts/release.ts notes",
    "release": "bun scripts/release.ts publish"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "zod": "^3.24.1"
  }
}
